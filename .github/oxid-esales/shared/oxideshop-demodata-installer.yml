# {{ $ids := "oedemodatainstaller" }}ids: {{ print $ids }}
# {{ $org := "oxid-esales" }}organisation: {{ print $org }}
# {{ $name := "oxideshop-demodata-installer" }}name: {{ print $name }}
phpunit:
  title: '{{ print $name }}-unit'
  container:
    options: ''
  configuration: '/var/www/vendor/{{ print $org }}/oxideshop-ce/phpunit.xml'  # ToDo: Replace this by a config from the module itself
  test: '/var/www/vendor/{{ print $org }}/{{ print $name }}/tests/Unit'
  additional_options: '--bootstrap /var/www/source/bootstrap.php'
  output:
    files: |
      source/composer.json
      source/unit_coverage_{{ print $ids }}.xml
      source/unit.cov
      source/source/log/oxideshop.log
      data/php/logs/error_log.txt
      files.txt
    prefix: 'phpunit-unit-{{ print $name }}-artifacts'
  logfile:
    prefix: 'phpunit-unit-{{ print $name }}'
  coverage:
    prefix: 'coverage-reports-phpunit-unit-{{ print $name }}'
    path: ''

styles:
  title: '{{ print $name }}'
  path: '{{ print $ids }}'
  module_ids: '{{ print $ids }}'
